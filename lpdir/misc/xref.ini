%   File   :  /usr/bs/lpdir/misc/xref.ini
%   Author : Bernard Silver
%   Updated: Tue Oct 29 21:04:01 1985
%   Purpose:  Initializations for the benefit of XREF.

 % Default definitions for stored things

already_said(_,_,_,_).

asserted(_).

auto_rule(_,_).

bad_schema(_).

c_d(_,_,_,_,_).

difficult_once(_,_).

integral(_).

just_created(_).

known_method_auto(_,_,_,_,_,_,_).

known_method_schema(_,_,_,_,_,_,_,_,_).

last_equation(_).

last_example(_,_).

new_functor(_,_).

new_rule_stored(_).

rule(_,_,_,_).

schema(_).

schema_abandon(_,_,_).

schema_used(_).

store_we(_).

temp_file(_).

tidy_last_equation(_).

unknown(_).

user_rule(_,_,_,_,_,_,_).

vote(_,_,_,_).
